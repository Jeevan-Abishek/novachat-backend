# Server
NODE_ENV=production
PORT=4000
INSTANCE_ID=server-1
CLIENT_URL=https://app.novachat.io

# MongoDB (Replica Set for high availability)
MONGO_URI=mongodb://mongo1:27017,mongo2:27017,mongo3:27017/novachat?replicaSet=rs0

# Redis (Pub/Sub + Socket.IO adapter + caching)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=30d

# Cloudinary (media storage)
CLOUDINARY_CLOUD_NAME=novachat
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email (OTP / verification / password reset) — sent via Resend's HTTP API
# (not SMTP — Render blocks/throttles outbound SMTP ports, HTTP is reliable)
# Sign up free at https://resend.com and create an API key
EMAIL_FROM=NovaChat <no-reply@novachat.io>
RESEND_API_KEY=re_your_api_key_here

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Bcrypt
BCRYPT_SALT_ROUNDS=12
